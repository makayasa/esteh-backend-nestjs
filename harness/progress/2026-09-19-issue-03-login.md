# 2026-09-19 — issue-03-login

- Branch main; feature issue-03-login; GitHub #3.
- Pengguna menyetujui CLI/HTTP/clock/DB audit seams sebelum test ditulis.

## Completed

CLI bootstrap stdin tanpa default/registrasi publik; Argon2id native Node 26,
login dengan token HMAC dari nonce acak dan secret runtime, DB hanya hash token.
Prisma migration Account/Session/Audit/Idempotency/LoginLimit; transaksi, row lock,
audit tanpa secret, rate limit persisten. HTTP validation, generic internal errors,
16 KiB limit dan OpenAPI/Swagger terlindung admin. Kontrak docs/api.md.

## Verification / evidence

- Red: `node scripts/test-api.mjs` awal gagal karena CLI belum ada, lalu login 404.
- Green: `npm run test:api` exit 0. PostgreSQL 17.11 container terisolasi fresh;
  CLI bootstrap berhasil, bootstrap kedua ditolak; login/me; hash Argon2id dan
  token hanya hash; Swagger unauthorized 401/admin 200; payload invalid 400;
  4 concurrent login retry token sama, satu Session/Audit; password salah,
  unknown/nonaktif 401; key sama payload valid berubah 409; tidak ada register;
  trigger fault audit menghasilkan 500 tanpa session/idempotency parsial;
  rate limit 429; password/token tidak muncul pada logs/audit.
- `npm run verify` exit 0 (13 tooling, 1 unit, 2 e2e). Lint warning JSON
  stringification diperbaiki; `npm run lint` ulang exit 0 tanpa warning.
- @nestjs/swagger 12.0.1 peer NestJS ^12 terverifikasi, dipin lockfile.
- Test DB dihapus hanya milik runner; .env repo tidak diubah.

## Broken or unverified

Expiry/logout milik #4, akun #5, katalog #6/#7 belum dikerjakan pada evidence ini.
Native Argon2 Node 26 berstatus experimental. npm audit menemukan 9 advisories
(2 low, 1 moderate, 6 high) pada dependency tree Prisma/Nest mau yang sudah ada;
fix rekomendasi merupakan downgrade major. Tidak melakukan downgrade tanpa review.
Bukan siap internet/produksi. Config Compose kini memerlukan SESSION_SECRET;
smoke Compose versi auth belum dilakukan. Diff gate belum ada commit base.

## Next

Lanjut #4 waktu terkendali dan logout; semua acceptance DB issue #3 sudah diuji.
Rebuild graph setelah interface issue #3–#7 stabil. Lengkapi smoke Compose akhir.

## Working tree / commits

Tanpa commit/push/deploy. Bukti remote hanya komentar, file masih lokal untracked.
